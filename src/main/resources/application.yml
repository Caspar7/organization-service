server:
  port: 4444
spring:
  application:
    name: organization-service
  profiles:
    active: ${env}

#admin client config
management:
  endpoint:
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include: "*"